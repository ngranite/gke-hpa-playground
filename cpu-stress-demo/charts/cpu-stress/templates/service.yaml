apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpu-stress.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "cpu-stress.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
